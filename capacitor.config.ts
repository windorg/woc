import { CapacitorConfig } from '@capacitor/cli'

const config: CapacitorConfig = {
  appId: 'me.windofchange',
  appName: 'WOC',
  webDir: 'out',
  bundledWebRuntime: false,
}

export default config
